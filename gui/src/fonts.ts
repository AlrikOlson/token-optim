/* Self-hosted period faces (design law, think:63):
   Archivo — the display grotesque (900 = stamps/mastheads), now the
   variable face (wght + wdth axes) so display sizes can pull wide
   (gui-11 faux-optical sizing; Archivo carries no opsz axis);
   IBM Plex Mono — the typewriter-with-a-pension evidence face. */
import "@fontsource-variable/archivo/wdth.css";
import "@fontsource/ibm-plex-mono/400.css";
import "@fontsource/ibm-plex-mono/600.css";
